name: Docker Build

on:
  workflow_dispatch:

jobs:
    docker-build:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v7

            - name: Build Docker image
              run: docker build -t nodegoat:ci .