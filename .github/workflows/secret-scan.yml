name: Secret Scan

on:
    workflow_dispatch:

jobs:
    secret-scan:
        runs-on: ubuntu-latest

        steps:
            - name: checkout code
              uses: actions/checkout@v7

            - name: Run Gitleaks
              uses: gitleaks/gitleaks-action@v2